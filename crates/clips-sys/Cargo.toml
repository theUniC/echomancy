[package]
name = "clips-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to CLIPS 6.4.2 — PoC/spike only"
links = "clips"

[lib]
name = "clips_sys"

[build-dependencies]
cc = { workspace = true }

[dev-dependencies]
